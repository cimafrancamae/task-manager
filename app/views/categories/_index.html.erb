
<% @categories.each do |category| %>
  <div class="bg-white p-4 mb-4 rounded shadow">
    <h2 class="text-lg font-semibold mb-2"><%= link_to category.name, category %></h2>
    <!-- Additional category details or actions -->
  </div>
<% end %>
